name: domhmm-test
channels:
  - conda-forge
  - defaults
dependencies:
  # Base depends
  - python=3.9
  - pip

  # MDAnalysis
  - MDAnalysis

  # Functionality
  - hmmlearn
  - networkx

  # Testing
  - pytest
  - pytest-cov
  - pytest-xdist
  - codecov

  # Pip-only installs
#  - pip:
#    - codecov
